![alt text](image.png)

I am not sure whether this is a problem or a security issue, but I cannot copy
text from OpenCode inside SSH and paste it on the computer.

- Prefer Clang over GCC for installation; Clang also covers C++.
- Future languages: Ruby, Rust, Java and Zig.
- Later: R and Kotlin.
- Possible future support: Bun and C#.

Potential data tools:

- SQLite
- PostgreSQL
- Redis?

The following screenshot is a historical visual note, not a product contract:

![alt text](image-1.png)

```text
======================================
                 APP DETAILS
                    neovim
                 Modal editor

              State: available
            Source: not installed
           Version: not detected
     Mobdesk configuration: not_applied
        Paths: /root/.config/nvim,
               /root/.config/nvim/lua,
               /root/.config/nvim/lua/config,
               /root/.config/nvim/lua/plugins
          Managed plugins: 3
 Storage: app 15-30 MB; dependencies 0-20 MB;
                    config 0-2 MB
         Estimated total: 15-52 MB

                   Actions
                 [ Install ]
                 [ Uninstall ]
       Install the app before uninstalling it
       [ Add Mobdesk configuration ]
       Install the app before applying configuration
       [ Remove Mobdesk configuration ]
          Configuration has not been applied
                    [ Close ]
```
